name: GitHub → Discord notifications (reusable)

on:
  workflow_call:
    inputs:
      event_name:
        required: true
        type: string
      repository:
        required: true
        type: string
      actor:
        required: true
        type: string

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send notification to Discord
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_GITHUB_ACTIVITY }}
        with:
          args: >
            🔔 **GitHub Event**
            Repository: **${{ inputs.repository }}**
            Event: **${{ inputs.event_name }}**
            Actor: **${{ inputs.actor }}**
